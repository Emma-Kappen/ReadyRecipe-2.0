# ReadyRecipe Web (minimal)

Run development server:

```bash
cd web
npm install
npm run dev
```

The app expects the backend at `http://localhost:8080` and uses `/api/login` and `/api/signup` endpoints.
