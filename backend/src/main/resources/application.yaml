spring:
  application:
    name: ReadyRecipe Backend
  datasource:
    url: jdbc:postgresql://localhost:5433/readyrecipe_test?timezone=Asia/Kolkata
    username: user
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    host: localhost
    port: 6379

server:
  port: 8080

logging:
  level:
    com.readyrecipe: DEBUG
